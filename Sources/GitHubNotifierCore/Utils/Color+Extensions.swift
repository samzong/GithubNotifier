import SwiftUI

extension Color {
    public static let notificationBackground = Color(nsColor: .controlBackgroundColor)
    public static let notificationHover = Color(nsColor: .selectedContentBackgroundColor)
}
